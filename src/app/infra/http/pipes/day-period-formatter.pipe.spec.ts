import { DayPeriodFormatterPipe } from './day-period-formatter.pipe';

describe('DayPeriodFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new DayPeriodFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
